DB_NAME=mdd
DB_USER=mdd_user
DB_PASSWORD=change_me
DB_ROOT_PASSWORD=change_me_too
DB_HOST_PORT=3306

# Clé de signature HMAC en Base64, 256 bits minimum : openssl rand -base64 32
JWT_SECRET=change_me_base64_256_bits
# Durée de validité d'un jeton, en millisecondes (86400000 = 24 h)
JWT_EXPIRATION_MS=86400000
